Perfect. Here is the phase-wise, file-by-file implementation checklist focused only on product export PDF + Excel async flow.
I’ve incorporated your updates:
- WebP in PDF -> skip embed + placeholder text Image format not supported
- Polling (not webhook)
- No feature flags
- Minimum 3 retries on failure
---
Phase 1 — Data Model + Shared Contracts (Backend first)
- Create src/models/exportJob.ts
  - Define job schema/type:
  - _id, target, targetId, workspaceId, requestedBySub, requestedByUserId, format (pdf|excel), status (queued|processing|completed|failed), attempts, fileName, contentType, s3Key, errorMessage, createdAt, startedAt, completedAt, expiresAt
  - Export collection constant (e.g., EXPORT_JOB_COLLECTION)
- Create src/types/export-job.ts
  - Shared backend contracts for:
  - enqueue payload/response
  - queue message payload
  - status/list/download response shapes
- Create src/utils/productExportJobs.ts
  - Job DB helper functions:
  - ensureProductExportJobIndexes()
  - createQueuedJob(...)
  - markJobProcessing(...)
  - markJobCompleted(...)
  - markJobFailed(...)
  - getJobByIdForUser(...)
  - listJobsForUser(...)
  - Include indexes (including TTL if desired for job docs)
- Modify src/utils/statusCodes.ts
  - Add ACCEPTED = 202
- Modify src/utils/responseWrapper.ts
  - Add accepted(...) helper (or use custom(202, ...) consistently)
---
Phase 2 — Infra (SQS + Worker + IAM + Retry Policy)
- Modify template.yaml
  - Add ProductExportQueueDLQ (AWS::SQS::Queue)
  - Add ProductExportQueue with redrive policy
    - maxReceiveCount: 4 (1 initial + 3 retries)
  - Add new worker function:
    - ProcessProductExportJobFunction
    - SQS event source mapping from ProductExportQueue
    - BatchSize: 1
    - high timeout + memory tuning
    - FunctionResponseTypes: [ReportBatchItemFailures] (recommended)
  - Add IAM policies:
    - enqueue functions -> sqs:SendMessage on queue ARN
    - worker -> SQS consume/delete + S3 put/get on export prefix
  - Add env vars for queue URL and export prefix (and optional polling URL expiry config)
- Modify src/package.json
  - Add dependency: @aws-sdk/client-sqs
- Optional (if used in local runs) Modify env.json
  - Add any new envs needed for local testing defaults
---
Phase 3 — Enqueue + Status + Download APIs (Product only)
- Repurpose/Modify src/controllers/products/exportProductPDF.ts
  - Change from direct file response to enqueue job flow for PDF
- Repurpose/Modify src/controllers/products/exportProductExcel.ts
  - Change from direct file response to enqueue job flow for Excel
- Create src/controllers/products/getProductExportJob.ts
  - GET /products/exports/jobs/{jobId}
  - Owner-only access; return job status/detail
- Create src/controllers/products/getProductExportJobs.ts
  - GET /products/exports/jobs
  - Current user’s jobs only; pagination/filter support
- Create src/controllers/products/downloadProductExportJob.ts
  - GET /products/exports/jobs/{jobId}/download
  - Only when completed; return signed URL/redirect
- Modify template.yaml
  - Route mappings:
    - POST /products/{productId}/exports/pdf
    - POST /products/{productId}/exports/excel
    - GET /products/exports/jobs
    - GET /products/exports/jobs/{jobId}
    - GET /products/exports/jobs/{jobId}/download
- Security checks in these controllers
  - Validate product exists
  - Validate requester is allowed (resolve user by auth.payload.sub -> user/workspace and match product workspace)
  - Only job creator can read/download job
---
Phase 4 — Worker + Export Engine Hardening (Timeout/Memory + Retries)
- Create src/controllers/products/processProductExportJob.ts
  - SQS Lambda handler:
  - parse queue message (jobId)
  - lock/claim queued job -> processing
  - increment/store attempts from receive count
  - generate file (PDF/Excel)
  - upload to S3
  - update job to completed/failed
  - throw on retryable failures so SQS retries
  - final failure persists status + error message
- Create src/utils/productExportWorker.ts (optional but recommended)
  - Worker orchestration helpers (format switch, retryability classification, filename generation)
- Modify src/utils/s3-storage.ts
  - Add helper(s):
  - upload buffer to exports prefix
  - generate signed download URL for export object
- Modify src/utils/exportPDF.ts
  - Replace WebP placeholder text from WEBP not supported -> Image format not supported
  - Keep export running (do not fail whole job)
  - Reduce memory spikes:
    - replace unbounded Promise.all preloading with bounded concurrency/chunking or on-demand embed cache
  - Keep “image unavailable” fallback behavior
- Modify src/utils/exportExcel.ts
  - Add actual image embedding (currently mostly text URLs)
  - Resolve signed URLs/keys similarly to PDF logic
  - For unsupported format/fetch failures: place text placeholder in cell and continue
  - Use bounded image fetch/embed concurrency to avoid OOM
---
Phase 5 — Frontend API Migration + Polling UX + Notifications
- Modify hooks/product/useExportProductPDF.ts
  - Switch to enqueue POST /api/products/{productId}/exports/pdf
  - Return { jobId, status }, not blob download
- Modify hooks/product/useExportProductExcel.ts
  - Switch to enqueue POST /api/products/{productId}/exports/excel
- Create hooks/product/useGetProductExportJobs.ts
  - Poll list endpoint; refetchInterval when jobs in queued/processing
- Create hooks/product/useGetProductExportJob.ts
  - Optional per-job polling hook for detail flow
- Create hooks/product/useDownloadProductExportJob.ts (or inline action)
  - Call download endpoint and trigger browser download via URL
- Create types/export-job.ts
  - UI job types and API response types
- Modify features/workspace/products/DialogExportProductPDF.tsx
  - Button text/copy: “Start export”
  - On success: toast “Export started” + close dialog
- Modify features/workspace/products/DialogExportProductExcel.tsx
  - Same enqueue UX
- Modify features/workspace/products/product/ProductHeader.tsx
  - Export dropdown should queue jobs (not immediate file download)
  - Show in-progress state as “Starting export...” / “Queued...”
- Create app/(app)/products/exports/page.tsx
  - Jobs table/list:
  - format, product, requested time, status, attempts, error
  - download button for completed jobs
  - auto-refresh while active jobs exist
- Create components/common/ProductExportJobNotifier.tsx
  - Poll recent jobs and toast transition events:
  - processing -> completed
  - processing -> failed
  - Prevent duplicate toasts per job/status in same session
- Modify app/(app)/layout.tsx
  - Mount ProductExportJobNotifier globally inside authenticated app shell
- Modify components/common/AppSidebar.tsx (optional but recommended)
  - Add nav item: /products/exports
- Modify components/common/AppHeader.tsx
  - Add label/icon handling for /products/exports
- Optional app/(app)/products/page.tsx
  - Add “View Exports” shortcut button next to “Create Product”
---
Phase 6 — Validation Checklist (before coding reports later)
- Backend validation
  - Enqueue returns 202 + jobId
  - Worker transitions states correctly
  - 3 retries happen (plus initial try) and then terminal failure
  - Owner-only access to job status/download
  - S3 object expires after 24h (lifecycle rule)
- Export behavior validation
  - PDF with WebP images shows placeholder text Image format not supported
  - PDF/Excel continue even with some broken/unsupported images
  - Large image-heavy products complete without Lambda OOM/timeouts (after tuning)
- Frontend validation
  - Enqueue toast appears immediately
  - Polling updates statuses correctly
  - Completed/failed toasts appear once
  - Download button works only for completed jobs
---
